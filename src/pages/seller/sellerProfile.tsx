import React from 'react';
import SellerProfileView from '@/views/seller/SellerProfile'; // ✅ Make sure the file name matches case

const SellerProfile: React.FC = () => {
  return <SellerProfileView />;
};

export default SellerProfile;
